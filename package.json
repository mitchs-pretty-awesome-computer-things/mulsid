{
	"version": "0.0.1",
	"name": "@mpact/mulsid",
	"description": "Mini Unique Lexographically-Sortable IDs",
	"license": "MIT",
	"type": "module",
	"main": "./dist/mod.js",
	"types": "./dist/mod.d.ts",
	"scripts": {
		"fmt": "bunx biome check --write",
		"build": "bunx tsc -p tsconfig.build.json",
		"clean": "rm -rf ./dist",
		"prepublish": "bun run clean && bun run build"
	},
	"files": ["dist", "README.md", "LICENSE"],
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	}
}
